module.exports = {
    name: 'help',
    execute(message, args, Discord, client) {

        const helpEmbed = new Discord.MessageEmbed()
        .setTitle(`MedalSMP`)
        .setDescription(``)
        .setFooter(`MedalSMP`)
        .setTimestamp();
    }
}